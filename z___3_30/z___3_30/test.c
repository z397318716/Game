#include<stdio.h>


// inline   优化调用函数  (针对函数语句比较少  调用函数的话相比于直接写在主函数里)
inline int dff(){

}

void init()



int main(){

	// 数位遍历   进制转换
	int i, n;
	for (i = n; i; i /= 10)  //i%10 将会对n的每一位进行遍历
		// i%

		// strncmp 与 strcmp   有 n 的只比较前 n 个
		// strcpy  strcat strlen  都有 n 版的  

		//  if(0 == a)  可以写成 if( !a )

		// putchar(ch | 1 << 5)   ch 或  10000(第五位 置1)
		// putchar(ch &~ (1<<5))  ch 与        (第五位 置 0 )

	system("pause");
	return 0;
}